# Runtime dependencies installed from PyPI.
#
# NOTE: vllm is deliberately NOT declared here. PPU is CUDA-source-compatible
# but not binary-compatible: a PyPI vllm wheel ships compiled CUDA binaries
# (_C, _moe_C, vllm_flash_attn, ...) that cannot run on PPU. vLLM must be
# PPU-built from source (or a PPU wheel); vllm_sail/compat.py validates the
# installed version (>=0.30.0,<0.31.0) at runtime instead.
apache-tvm-ffi==0.1.9
fastsafetensors>=0.3.2
tokenspeed-mla==0.1.2

# PPU SDK dependencies — supplied by the SDK and NOT installable from PyPI.
# Their availability is checked at runtime: acext, deep_gemm, flash_attn,
# flash_attn_3.
