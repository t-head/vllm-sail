# Runtime patch manifest

Generated by `python tools/patch_manifest.py`; do not edit by hand.

| Target | Kind | Reason | Affected versions | Remove when |
|---|---|---|---|---|
| vllm.config.compilation.CompilationConfig.set_splitting_ops_for_v1 | class | The PPU sparse-attention indexer torch op must be a piecewise-cudagraph splitting point exactly like upstream's vllm::sparse_attn_indexer. The fork adds it to CompilationConfig._attention_ops; without it the op is traced into a piecewise graph segment instead of being an attention boundary. | >=0.30.0,<0.31.0 | upstream includes vllm::ppu_sparse_attn_indexer in CompilationConfig._attention_ops itself, or the PPU indexer stops registering its own torch op and reuses vllm::sparse_attn_indexer. |
| vllm.config.kernel.MoEBackend | value | KernelConfig validates moe_backend against the MoEBackend Literal, which upstream does not extend. The PPU backend names must pass validation for --moe-backend / --kernel-config CLI parity; the env-var knob VLLM_PPU_MOE_BACKEND bypasses validation and remains the primary knob. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel (the plugin's #1 upstream RFC), or accepts the ppu_* members into the MoEBackend Literal. |
| vllm.distributed.device_communicators.all2all.DeepEPLLAll2AllManager._make_all2all_kwargs | class | PPU's deep_ep build does not support enable_shrink yet (fork FIXME), so the DeepEP low-latency buffer kwargs must not carry it. Upstream passes enable_shrink=self.support_fault_tolerance unconditionally. | >=0.30.0,<0.31.0 | the PPU deep_ep build implements enable_shrink (shrinkable buffers for fault tolerance), at which point the upstream kwarg can flow through unchanged. |
| vllm.lora.layers.utils.try_get_optimal_moe_config | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.custom_op.CustomOp.dispatch_forward | class | Upstream's platform ladder has no PPU branch, so an op that overrides forward_ppu never gets called. This wrapper delegates to upstream and only redirects when upstream selected forward_cuda AND the op class actually overrides forward_ppu. | >=0.30.0,<0.31.0 | upstream's dispatch ladder gains a PPU (or vendor-hook) branch. |
| vllm.model_executor.custom_op.CustomOp.forward_ppu | class | Provides the default PPU implementation for every CustomOp: PPU ops are assumed CUDA-compatible, so the default delegates to forward_cuda. Ops with a genuinely different PPU kernel override this method. | >=0.30.0,<0.31.0 | upstream adds a forward_ppu hook, or PPU stops needing per-op overrides. |
| vllm.model_executor.kernels.mhc.tilelang._hc_prenorm_gemm_outputs | module | PPU MHC uses SAIL DeepGEMM, one split and zeroed accumulators. | >=0.30.0,<0.31.0 | The upstream MHC prenorm helper supports platform dispatch. |
| vllm.model_executor.layers.attention.mla_attention.MLAAttention.forward_impl | class | PPU FIXME: WA for deepseek dp + ep OOM on PPU — the fork skips the profile-run torch.empty scratch allocation in forward_impl. The allocation is mid-method, so this is a verbatim upstream body with only the fork's change marked. | >=0.30.0,<0.31.0 | the PPU OOM workaround is root-caused upstream-side, or upstream stops allocating the profile-run scratch buffer. |
| vllm.model_executor.layers.attention.mla_attention._get_kv_b_proj_input_dtype | module | SAIL ACEXT quantizes floating activations internally for INT8 kv_b_proj weights. | >=0.30.0,<0.31.0 | The upstream input-dtype helper recognizes SAIL INT8 linear kernels. |
| vllm.model_executor.layers.fused_moe.all2all_utils.maybe_make_prepare_finalize | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.fused_moe.all2all_utils.maybe_roundup_layer_hidden_size | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.fused_moe.config._get_config_dtype_str | module | PPU int8 W8A8 MoE flows look up tuned configs by the name int8_w8a8; upstream's _get_config_dtype_str has no use_int8_w8a8 flag and never returns it. | >=0.30.0,<0.31.0 | upstream adds use_int8_w8a8 to _get_config_dtype_str itself. |
| vllm.model_executor.layers.fused_moe.config.int8_w8a8_moe_quant_config | module | The fork threads gemm1_clamp_limit through int8_w8a8_moe_quant_config and builds the PPU int8 MoE quant config with the swiglu limit in that slot; the PPU int8 experts read gemm1_clamp_limit. | >=0.30.0,<0.31.0 | upstream threads gemm1_clamp_limit/swiglu_limit through the int8 MoE quant-config builders itself. |
| vllm.model_executor.layers.fused_moe.experts.triton_moe.TritonExperts.apply | class | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.experts.triton_moe.TritonExperts.moe_sum | class | The fork switches TritonExperts.moe_sum to a Triton reduction for more than 1024 tokens on PPU; upstream's C++ moe_sum is the slow path there. | >=0.30.0,<0.31.0 | upstream's moe_sum is fast on PPU for large token counts, or the fork's moe_sum_reduce_triton kernel is upstreamed and selected by platform. |
| vllm.model_executor.layers.fused_moe.experts.triton_moe.invoke_fused_moe_triton_kernel | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.experts.triton_moe.invoke_fused_moe_wna16_triton_kernel | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.experts.triton_moe.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.experts.triton_moe.try_get_optimal_moe_config | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.dispatch_fused_moe_kernel | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.fused_experts_impl | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.get_config_file_name | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.get_moe_configs | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.get_moe_wna16_block_config | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.invoke_fused_moe_triton_kernel | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.invoke_fused_moe_wna16_triton_kernel | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.fused_moe.should_moe_wna16_use_cuda | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.fused_moe.try_get_optimal_moe_config | module | PPU tuned configurations require per-projection launch parameters, VALU and PPU Triton kernels. | >=0.30.0,<0.31.0 | Upstream supports registration of the complete Triton MoE launch strategy. |
| vllm.model_executor.layers.fused_moe.modular_kernel.FusedMoEExperts._supports_bias | class | PPU expert classes declare whether their GEMM supports expert bias. | >=0.30.0,<0.31.0 | FusedMoEExperts exposes a bias capability method. |
| vllm.model_executor.layers.fused_moe.modular_kernel.FusedMoEExperts.is_supported_config | class | Explicit and automatic DeepGEMM selection must reject unsupported bias. | >=0.30.0,<0.31.0 | FusedMoEExperts checks its bias capability during configuration selection. |
| vllm.model_executor.layers.fused_moe.modular_kernel.FusedMoEExpertsModular.moe_problem_size | class | PPU's MXFP4 experts pack two e2m1 values per uint8, so the trailing activation dimension is hidden_size // 2; upstream derives K from it unchanged and the PPU experts' workspace/shape derivation would run with a halved K. | >=0.30.0,<0.31.0 | upstream derives K from the weight/quant metadata instead of the packed activation extent, or the PPU MXFP4 experts override moe_problem_size themselves. |
| vllm.model_executor.layers.fused_moe.oracle.fp8._get_priority_backends | module | The FP8 oracle candidate list has no PPU DeepGEMM backends. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.fp8.backend_to_kernel_cls | module | The FP8 oracle cannot resolve plugin-owned PPU expert classes. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.fp8.convert_to_fp8_moe_kernel_format | module | PPU FP8 experts require block weight preparation or unchanged channelwise tensors. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.fp8.make_fp8_moe_quant_config | module | FP8 channelwise quantization must preserve SwiGLU alpha and beta. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.fp8.map_fp8_backend | module | The public ppu_deep_gemm string has no upstream FP8 member mapping. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.fp8.maybe_make_prepare_finalize | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.fused_moe.oracle.fp8.select_fp8_moe_backend | module | Explicit PPU FP8 selection must choose the batched PPU expert when needed and honor the plugin-specific DeepGEMM environment override. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.int8._get_priority_backends | module | The INT8 oracle has no registration API for PPU DeepGEMM and Acext experts, so PPU needs its own ordered candidate list. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.int8.backend_to_kernel_cls | module | The INT8 oracle cannot resolve plugin-owned PPU expert classes. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.int8.convert_to_int8_moe_kernel_format | module | PPU DeepGEMM and Acext consume canonical INT8 MoE weights directly, but upstream rejects their plugin-added backend enum members during process_weights_after_loading. | >=0.30.0,<0.31.0 | upstream accepts out-of-tree INT8 backends in its weight-conversion hook, or exposes a backend-specific conversion registry. |
| vllm.model_executor.layers.fused_moe.oracle.int8.make_int8_moe_quant_config | module | The fork threads gemm1_clamp_limit through int8_w8a8_moe_quant_config and builds the PPU int8 MoE quant config with the swiglu limit in that slot; the PPU int8 experts read gemm1_clamp_limit. | >=0.30.0,<0.31.0 | upstream threads gemm1_clamp_limit/swiglu_limit through the int8 MoE quant-config builders itself. |
| vllm.model_executor.layers.fused_moe.oracle.int8.map_int8_backend | module | The public PPU backend strings have no upstream INT8 member mapping. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.int8.select_int8_moe_backend | module | Batched INT8 activations require the batched PPU DeepGEMM expert and must reject standard-only Triton and Acext selections. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16._backend_incompatibility_reason | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16._get_priority_backends | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16.backend_to_kernel_cls | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16.check_moe_marlin_supports_config | module | Preloaded WNA16 consumers must observe the PPU Marlin capability gate. | >=0.30.0,<0.31.0 | WNA16 consumers resolve Marlin capabilities through the provider module. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16.convert_to_wna16_moe_kernel_format | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16.make_wna16_moe_kernel | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16.map_wna16_backend | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.int_wna16.select_wna16_moe_backend | module | The WNA16 oracle needs the plugin's PPU DeepGEMM experts and packed weight layout. | >=0.30.0,<0.31.0 | The MoE oracle supports registering plugin WNA16 backends. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4._backend_activation_key | module | PPU MXFP4 W4A4 backends need the kMxfp4Dynamic activation key. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4._get_priority_backends | module | The generic MXFP4 candidate list has no PPU DeepGEMM backends. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4._get_priority_backends_for_gpt_oss | module | The GPT-OSS MXFP4 candidate list has no PPU DeepGEMM backends. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.backend_to_kernel_cls | module | The MXFP4 oracle cannot resolve plugin-owned PPU expert classes. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.convert_gpt_oss_weight_to_mxfp4_moe_kernel_format | module | The PPU MXFP4 backends need an ocp_mx quant config (block_shape [1,16]), deep_gemm.preprocess_mxfp4_scales weight-scale preprocessing, and 32-alignment of hidden/intermediate sizes; upstream's oracle has no branch for them. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.convert_weight_to_mxfp4_moe_kernel_format | module | The PPU MXFP4 backends need an ocp_mx quant config (block_shape [1,16]), deep_gemm.preprocess_mxfp4_scales weight-scale preprocessing, and 32-alignment of hidden/intermediate sizes; upstream's oracle has no branch for them. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.make_mxfp4_moe_quant_config | module | The PPU MXFP4 backends need an ocp_mx quant config (block_shape [1,16]), deep_gemm.preprocess_mxfp4_scales weight-scale preprocessing, and 32-alignment of hidden/intermediate sizes; upstream's oracle has no branch for them. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.map_mxfp4_backend | module | The public ppu_deep_gemm string has no upstream MXFP4 member mapping. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.maybe_make_prepare_finalize | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.mxfp4_round_up_hidden_size_and_intermediate_size | module | The PPU MXFP4 backends need an ocp_mx quant config (block_shape [1,16]), deep_gemm.preprocess_mxfp4_scales weight-scale preprocessing, and 32-alignment of hidden/intermediate sizes; upstream's oracle has no branch for them. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.select_deepseek_v4_mxfp4_moe_backend | module | Preserve PPU W4A4/W4A16 selection through the 0.30 MXFP4 kernel factory. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp4.select_mxfp4_moe_backend | module | Resolve explicit PPU W4A4 and W4A16 standard/batched candidates with activation filtering. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.mxfp8.backend_to_kernel_cls | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.fused_moe.oracle.nvfp4.maybe_make_prepare_finalize | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.fused_moe.oracle.unquantized._get_priority_backends | module | The unquantized oracle has no registration API for PPU DeepGEMM and Acext experts, so PPU needs its own ordered candidate list. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.unquantized.backend_to_kernel_cls | module | The unquantized oracle cannot resolve plugin-owned PPU expert classes. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.unquantized.map_unquantized_backend | module | The public MoE backend strings have no upstream mapping to PPU members. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.oracle.unquantized.maybe_make_prepare_finalize | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.fused_moe.oracle.unquantized.select_unquantized_moe_backend | module | Explicit ppu_deep_gemm requests need the batched PPU member when the prepare/finalize path uses batched expert activations. | >=0.30.0,<0.31.0 | upstream gains a register_moe_backend() extension point mirroring register_linear_kernel(), which would delete this patch. |
| vllm.model_executor.layers.fused_moe.prepare_finalize.batched.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.prepare_finalize.flashinfer_nvlink_one_sided.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.prepare_finalize.flashinfer_nvlink_two_sided.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.prepare_finalize.naive_dp_ep.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.prepare_finalize.no_dp_ep.moe_kernel_quantize_input | module | Preloaded MoE consumers retain the upstream quantizer and bypass PPU MXFP4 input quantization. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.fused_moe.unquantized_fused_moe_method.select_unquantized_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.fused_moe.utils.moe_kernel_quantize_input | module | PPU MXFP4 activations are quantized with the plugin's downcast_to_mxfp4; upstream raises NotImplementedError for native mxfp4 input quantization on non-XPU platforms. | >=0.30.0,<0.31.0 | upstream's moe_kernel_quantize_input grows a PPU (or generic mxfp4 downcast) branch. |
| vllm.model_executor.layers.linear.dispatch_unquantized_gemm | module | PPU BF16 dense DeepGEMM needs a platform dispatch hook and fake implementation. | >=0.30.0,<0.31.0 | Unquantized dense GEMM supports backend registration. |
| vllm.model_executor.layers.mamba.gdn.qwen_gdn_linear_attn.ChunkGatedDeltaRule.__init__ | class | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.model_executor.layers.mamba.gdn.qwen_gdn_linear_attn.ChunkGatedDeltaRule.forward_pla | class | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.model_executor.layers.mamba.gdn.qwen_gdn_linear_attn._log_gdn_backend_decision | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.model_executor.layers.mamba.gdn.qwen_gdn_linear_attn._pla_prefill_supported | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.model_executor.layers.mamba.gdn.qwen_gdn_linear_attn._resolve_gdn_prefill_backend | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.model_executor.layers.mhc.HCHeadOp.forward_ppu | class | Fork's PPU CustomOp surface: MHC ops get an explicit forward_ppu that delegates to forward_cuda, matching the plugin's custom-op dispatch contract (forward_ppu selected whenever forward_cuda would be). | >=0.30.0,<0.31.0 | upstream adds forward_ppu to these ops, or the MHC ops stop being used on PPU. |
| vllm.model_executor.layers.mhc.MHCFusedPostPreOp.forward_ppu | class | Fork's PPU CustomOp surface: MHC ops get an explicit forward_ppu that delegates to forward_cuda, matching the plugin's custom-op dispatch contract (forward_ppu selected whenever forward_cuda would be). | >=0.30.0,<0.31.0 | upstream adds forward_ppu to these ops, or the MHC ops stop being used on PPU. |
| vllm.model_executor.layers.mhc.MHCPostOp.forward_ppu | class | Fork's PPU CustomOp surface: MHC ops get an explicit forward_ppu that delegates to forward_cuda, matching the plugin's custom-op dispatch contract (forward_ppu selected whenever forward_cuda would be). | >=0.30.0,<0.31.0 | upstream adds forward_ppu to these ops, or the MHC ops stop being used on PPU. |
| vllm.model_executor.layers.mhc.MHCPreOp.forward_ppu | class | Fork's PPU CustomOp surface: MHC ops get an explicit forward_ppu that delegates to forward_cuda, matching the plugin's custom-op dispatch contract (forward_ppu selected whenever forward_cuda would be). | >=0.30.0,<0.31.0 | upstream adds forward_ppu to these ops, or the MHC ops stop being used on PPU. |
| vllm.model_executor.layers.quantization.auto_awq.convert_to_wna16_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.auto_awq.make_wna16_moe_kernel | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.auto_awq.select_wna16_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.auto_gptq.convert_to_wna16_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.auto_gptq.make_wna16_moe_kernel | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.auto_gptq.select_wna16_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors.CompressedTensorsConfig.__init__ | class | The fork adds fp8_channelwise_layers to CompressedTensorsConfig: dense layers listed there are quantized FP8 channelwise on PPU instead of with the checkpoint's scheme; upstream has no such plumbing. | >=0.30.0,<0.31.0 | upstream grows fp8_channelwise_layers support in CompressedTensorsConfig, or the PPU mixed-precision checkpoints stop using it. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors.CompressedTensorsConfig._is_fp8_channelwise_layer | class | The fork adds fp8_channelwise_layers to CompressedTensorsConfig: dense layers listed there are quantized FP8 channelwise on PPU instead of with the checkpoint's scheme; upstream has no such plumbing. | >=0.30.0,<0.31.0 | upstream grows fp8_channelwise_layers support in CompressedTensorsConfig, or the PPU mixed-precision checkpoints stop using it. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors.CompressedTensorsConfig.from_config | class | The fork adds fp8_channelwise_layers to CompressedTensorsConfig: dense layers listed there are quantized FP8 channelwise on PPU instead of with the checkpoint's scheme; upstream has no such plumbing. | >=0.30.0,<0.31.0 | upstream grows fp8_channelwise_layers support in CompressedTensorsConfig, or the PPU mixed-precision checkpoints stop using it. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors.CompressedTensorsConfig.get_scheme | class | The fork adds fp8_channelwise_layers to CompressedTensorsConfig: dense layers listed there are quantized FP8 channelwise on PPU instead of with the checkpoint's scheme; upstream has no such plumbing. | >=0.30.0,<0.31.0 | upstream grows fp8_channelwise_layers support in CompressedTensorsConfig, or the PPU mixed-precision checkpoints stop using it. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors.CompressedTensorsKVCacheMethod.create_weights | class | The fork marks the KV-cache scale/zero-point Parameters requires_grad=False; upstream leaves the Parameter wrappers trainable. | >=0.30.0,<0.31.0 | upstream constructs the KV-cache scale/zero-point Parameters with requires_grad=False. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe.CompressedTensorsMoEMethod.get_moe_method | class | PPU runs compressed-tensors MXFP4 MoE through Mxfp4MoEMethod (whose PPU backend selection is patched separately); upstream picks the W4A4-MXFP4 method, which has no PPU experts. | >=0.30.0,<0.31.0 | upstream's compressed-tensors MXFP4 MoE path supports the PPU DeepGEMM experts directly. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_fp8.CompressedTensorsW8A8Fp8MoEMethod.get_fused_moe_quant_config | class | PPU FP8 experts need the model's SwiGLU alpha and beta parameters. | >=0.30.0,<0.31.0 | upstream threads swiglu_limit through the int8 MoE quant config itself. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_fp8.convert_to_fp8_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_fp8.make_fp8_moe_quant_config | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_fp8.select_fp8_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_int8.CompressedTensorsW8A8Int8MoEMethod.get_fused_moe_quant_config | class | The fork threads swiglu_limit into the int8 MoE quant config; on PPU it becomes gemm1_clamp_limit, which the PPU int8 experts read. Upstream drops it. | >=0.30.0,<0.31.0 | upstream threads swiglu_limit through the int8 MoE quant config itself. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_int8.convert_to_int8_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_int8.make_int8_moe_quant_config | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.compressed_tensors_moe.compressed_tensors_moe_w8a8_int8.select_int8_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.compressed_tensors.transform.module.dispatch_unquantized_gemm | module | PPU BF16 dense DeepGEMM needs a platform dispatch hook and fake implementation. | >=0.30.0,<0.31.0 | Unquantized dense GEMM supports backend registration. |
| vllm.model_executor.layers.quantization.fp8.convert_to_fp8_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.fp8.is_deep_gemm_supported | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. fp8 binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.fp8.make_fp8_moe_quant_config | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.fp8.select_fp8_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.input_quant_fp8.DeepGemmQuantScaleFMT | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. input_quant_fp8 binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.input_quant_fp8.QuantFP8.forward_cuda | class | PPU QuantFP8 consumes fused residual RMSNorm context and optional optimized group quantization. | >=0.30.0,<0.31.0 | Upstream QuantFP8 exposes a fused norm and platform group-quant hook. |
| vllm.model_executor.layers.quantization.input_quant_fp8.QuantFP8.set_fused_rmsnorm | class | PPU QuantFP8 consumes fused residual RMSNorm context and optional optimized group quantization. | >=0.30.0,<0.31.0 | Upstream QuantFP8 exposes a fused norm and platform group-quant hook. |
| vllm.model_executor.layers.quantization.input_quant_fp8.is_deep_gemm_e8m0_used | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. input_quant_fp8 binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.input_quant_fp8.is_deep_gemm_supported | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. input_quant_fp8 binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.modelopt.convert_to_fp8_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.modelopt.make_fp8_moe_quant_config | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.modelopt.select_fp8_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.mxfp4.GptOssMxfp4MoEMethod.__init__ | class | PPU runs MXFP4 as W4A4 (mxfp4 activations) on sm90+ and W4A16 on sm80; upstream selects without the kMxfp4Dynamic activation key, so the PPU DeepGEMM MXFP4 experts would never be picked. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.quantization.mxfp4.Mxfp4Config.__init__ | class | The fork adds fp8_channelwise_layers to Mxfp4Config (PPU dense layers quantized FP8 channelwise), reads 'ignore'/'fp8_channelwise_layers' in from_config, routes ignored RoutedExperts to UnquantizedFusedMoEMethod for mixed-precision checkpoints, and remaps the list in apply_vllm_mapper; upstream has none of this. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.quantization.mxfp4.Mxfp4Config.apply_vllm_mapper | class | The fork adds fp8_channelwise_layers to Mxfp4Config (PPU dense layers quantized FP8 channelwise), reads 'ignore'/'fp8_channelwise_layers' in from_config, routes ignored RoutedExperts to UnquantizedFusedMoEMethod for mixed-precision checkpoints, and remaps the list in apply_vllm_mapper; upstream has none of this. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.quantization.mxfp4.Mxfp4Config.from_config | class | The fork adds fp8_channelwise_layers to Mxfp4Config (PPU dense layers quantized FP8 channelwise), reads 'ignore'/'fp8_channelwise_layers' in from_config, routes ignored RoutedExperts to UnquantizedFusedMoEMethod for mixed-precision checkpoints, and remaps the list in apply_vllm_mapper; upstream has none of this. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.quantization.mxfp4.Mxfp4Config.get_quant_method | class | The fork adds fp8_channelwise_layers to Mxfp4Config (PPU dense layers quantized FP8 channelwise), reads 'ignore'/'fp8_channelwise_layers' in from_config, routes ignored RoutedExperts to UnquantizedFusedMoEMethod for mixed-precision checkpoints, and remaps the list in apply_vllm_mapper; upstream has none of this. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.quantization.mxfp4.Mxfp4MoEMethod.get_fused_moe_quant_config | class | The fork threads swiglu_alpha/swiglu_beta into the PPU MXFP4 quant config; upstream's get_fused_moe_quant_config does not pass them. | >=0.30.0,<0.31.0 | upstream grows first-class PPU MXFP4 support (fp8_channelwise_layers config plumbing, W4A4 selection, and PPU branches in the MXFP4 oracle), or the PPU MXFP4 flow is dropped. |
| vllm.model_executor.layers.quantization.mxfp4.convert_gpt_oss_weight_to_mxfp4_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.mxfp4.convert_weight_to_mxfp4_moe_kernel_format | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.mxfp4.make_mxfp4_moe_quant_config | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.mxfp4.mxfp4_round_up_hidden_size_and_intermediate_size | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.mxfp4.select_deepseek_v4_mxfp4_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.mxfp4.select_mxfp4_moe_backend | module | vLLM 0.30 captured this MoE oracle function before PPU registration, so the consumer would bypass PPU DeepGEMM selection or preparation. | >=0.30.0,<0.31.0 | vLLM imports MoE selectors through a registration API instead of capturing module-level aliases before plugin registration. |
| vllm.model_executor.layers.quantization.utils.fp8_utils.get_tma_aligned_size | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. fp8_utils binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.utils.fp8_utils.get_w8a8_block_fp8_configs | module | Upstream resolves its tuned-config directory relative to its own __file__ and offers no user-folder hook, so the FP8 block-quant configs this plugin ships for PPU-ZW810/ZW810E/ZW-M890P are never found and the kernel silently falls back to an untuned default. | >=0.30.0,<0.31.0 | upstream gives get_w8a8_block_fp8_configs a user-config-folder hook like fused_moe.get_moe_configs already has (envs.VLLM_TUNED_CONFIG_FOLDER). |
| vllm.model_executor.layers.quantization.utils.fp8_utils.is_deep_gemm_e8m0_used | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. fp8_utils binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.utils.fp8_utils.transform_sf_into_required_layout | module | PPU's DeepGEMM requires different TMA alignment and scale-factor layout transforms than CUDA's. fp8_utils binds these helpers from vllm.utils.deep_gemm at module scope; on PPU they must come from the PPU DeepGEMM wrapper instead, or FP8 GEMMs get wrong scale layouts or a CUDA-only support probe. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.model_executor.layers.quantization.utils.humming_utils.maybe_make_prepare_finalize | module | The all-to-all factory needs PPU DeepEP quantized dispatch and completion support. | >=0.30.0,<0.31.0 | The prepare/finalize factory supports plugin backend registration. |
| vllm.model_executor.layers.quantization.utils.int8_utils._per_token_group_quant_int8 | module | PPU's INT8 W8A8 dense path needs round-to-nearest quantization; the upstream kernel truncates, which costs accuracy. Adds a `use_rounding` tl.constexpr that defaults to False, so every existing caller keeps upstream behaviour exactly. | >=0.30.0,<0.31.0 | upstream's per-token-group INT8 quantization offers a rounding mode (e.g. a `rounding=` argument), making this copy unnecessary. |
| vllm.model_executor.layers.quantization.utils.int8_utils.per_token_group_quant_int8 | module | PPU needs to force the Triton path (the compiled torch.ops._C kernel is present but truncates) and to enable round-to-nearest. Adds `use_triton` and `use_rounding`, both defaulting to False so existing callers are unaffected. | >=0.30.0,<0.31.0 | upstream exposes a rounding mode and a way to force the Triton path, or PPU's compiled INT8 quant kernel gains round-to-nearest. |
| vllm.model_executor.layers.quantization.utils.marlin_utils.check_moe_marlin_supports_config | module | The fork's Marlin kernels are in-place edits of upstream .cu/.h sources that a plugin cannot ship; v1 restricts PPU to non-Marlin MoE paths (phase-1 spec 1.8, decision (c)). Delegates to upstream unless PPU has the opt-out env var left at its default (off). | >=0.30.0,<0.31.0 | the Marlin PPU kernel edits are upstreamed (or rebuilt inside vllm_sail._moe_C) and VLLM_PPU_ENABLE_MOE_MARLIN defaults on. |
| vllm.model_executor.layers.sparse_attn_indexer.SparseAttnIndexer.__init__ | class | PPU tolerates a missing DeepGEMM in SparseAttnIndexer with a warning (falling back to a less efficient PyTorch path), while upstream raises on CUDA. PPU inherits the CUDA raise via PlatformEnum.CUDA, so the fork's PPU branch must run first. Delegation cannot intercept the upstream raise, hence the verbatim body. | >=0.30.0,<0.31.0 | upstream downgrades the missing-DeepGEMM error to a warning, or PPU always ships DeepGEMM. |
| vllm.model_executor.layers.sparse_attn_indexer.SparseAttnIndexer.forward_native | class | Fork redirects SparseAttnIndexer.forward_native to forward_ppu on PPU before the CUDA branch, so the disabled-custom-op path also uses the PPU kernel; other platforms delegate to the upstream implementation. | >=0.30.0,<0.31.0 | upstream's forward_native grows a platform hook, or PPU stops overriding this op. |
| vllm.model_executor.layers.sparse_attn_indexer.SparseAttnIndexer.forward_ppu | class | PPU implementation of the sparse attention indexer, calling the PPU ppu_sparse_attn_indexer custom op registered by vllm_sail.ops. Upstream has no forward_ppu; installed additively and reached through the custom_op_dispatch patch (enabled path) or the forward_native redirect (disabled path). | >=0.30.0,<0.31.0 | upstream adds a forward_ppu hook and the PPU op moves into the dispatch ladder. |
| vllm.model_executor.layers.utils.dispatch_unquantized_gemm | module | PPU BF16 dense DeepGEMM needs a platform dispatch hook and fake implementation. | >=0.30.0,<0.31.0 | Unquantized dense GEMM supports backend registration. |
| vllm.model_executor.layers.vocab_parallel_embedding.dispatch_unquantized_gemm | module | PPU BF16 dense DeepGEMM needs a platform dispatch hook and fake implementation. | >=0.30.0,<0.31.0 | Unquantized dense GEMM supports backend registration. |
| vllm.model_executor.models.deepseek_mtp.DeepSeekMTP.__init__ | class | Adds the is_fp4_ckpt flag the load_weights patch keys the fused indexer-wk weight mapping on. Delegates to upstream unchanged. | >=0.30.0,<0.31.0 | upstream load_weights stops special-casing modelopt_fp4 indexer weights, making the flag unused. |
| vllm.model_executor.models.deepseek_mtp.DeepSeekMTP.load_weights | class | Restricts the fused indexer wk/weights_proj stacked-parameter mapping and the FP8 wk upcast loader to modelopt_fp4 checkpoints; non-fp4 PPU quant schemes load separate wk/weights_proj weights. Verbatim upstream body with two marked guards; both control method-local loader tables, so delegation is impossible. | >=0.30.0,<0.31.0 | upstream load_weights stops special-casing modelopt_fp4 indexer weights, making both guards no-ops. |
| vllm.model_executor.models.deepseek_v2.DeepseekV2Model.__init__ | class | Adds the is_fp4_ckpt flag the load_weights patch keys the fused indexer-wk weight mapping on. Delegates to upstream unchanged. | >=0.30.0,<0.31.0 | upstream load_weights stops special-casing modelopt_fp4 indexer weights, making the flag unused. |
| vllm.model_executor.models.deepseek_v2.DeepseekV2Model.load_weights | class | Restricts the fused indexer wk/weights_proj stacked-parameter mapping and the FP8 wk upcast loader to modelopt_fp4 checkpoints; non-fp4 PPU quant schemes load separate wk/weights_proj weights. Verbatim upstream body with two marked guards; both control method-local loader tables, so delegation is impossible. | >=0.30.0,<0.31.0 | upstream load_weights stops special-casing modelopt_fp4 indexer weights, making both guards no-ops. |
| vllm.model_executor.models.deepseek_v2.Indexer.__init__ | class | For non-modelopt_fp4 checkpoints PPU quant schemes need the indexer wk projection quantized, which the upstream fused wk_weights_proj cannot be; also adds the int8 indexer q-quant path for PPU without FP8 support. Delegates to upstream, then rebuilds the projection and sets the PPU flags. | >=0.30.0,<0.31.0 | upstream parameterises the Indexer wk projection on quant_config and exposes a platform hook for the q-quant format. |
| vllm.model_executor.models.deepseek_v2.Indexer.forward | class | Selects between the fused wk_weights_proj path (modelopt_fp4 only) and the separate wk/weights_proj projections installed by the Indexer.__init__ patch, and uses int8 q quantization on PPU without FP8 support. Verbatim upstream body with two marked branches; the changes sit mid-body with no delegation seam. | >=0.30.0,<0.31.0 | upstream parameterises the Indexer wk projection on quant_config and exposes a platform hook for the q-quant format. |
| vllm.model_executor.models.llama4.Llama4MoE.custom_routing_function | class | The fork wraps the Llama4 custom router in torch.compile so it fuses into the compiled model graph on PPU; without it the router is a graph break. Delegates by calling the compiled upstream function. | >=0.30.0,<0.31.0 | upstream applies torch.compile to Llama4MoE.custom_routing_function itself. |
| vllm.model_executor.models.qwen3.Qwen3DecoderLayer.forward | class | Fuse residual RMSNorm with supported QuantFP8 input quantization, with norm fallback for other linear kernels. | >=0.30.0,<0.31.0 | Upstream exposes a fused norm/quant producer-consumer interface. |
| vllm.model_executor.models.qwen3_moe.Qwen3MoeDecoderLayer.__init__ | class | Threads the per-layer quant config into attention, MoE block and MLP for mix_layer checkpoints. Delegates to upstream with a shallow VllmConfig copy carrying the requested quant config. | >=0.30.0,<0.31.0 | upstream threads a per-layer quant_config through Qwen3MoeDecoderLayer itself. |
| vllm.model_executor.models.qwen3_moe.Qwen3MoeModel.__init__ | class | Reads quantization_config.mix_layer from the HF config and builds those layers unquantized. Delegates to upstream, substituting a layer factory that applies the per-layer override only when mix_layer is non-empty. | >=0.30.0,<0.31.0 | upstream Qwen3MoeModel honours quantization_config.mix_layer itself. |
| vllm.model_executor.models.qwen3_moe.Qwen3MoeSparseMoeBlock.__init__ | class | Mixed-precision PPU checkpoints mark some layers mix_layer to keep them unquantized; that requires overriding vllm_config.quant_config per layer. Delegates to upstream with a shallow VllmConfig copy carrying the requested quant config. | >=0.30.0,<0.31.0 | upstream threads a per-layer quant_config through Qwen3MoeSparseMoeBlock itself. |
| vllm.model_executor.models.qwen3_next.th_nvtx_range_pop | module | Ports the fork's NVTX profiling scaffold for qwen3_next; no-op unless VLLM_PPU_NVTX_PROFILE is set and torch.cuda.nvtx imports. | >=0.30.0,<0.31.0 | the vllm_sail.profiling subpackage (Phase 6) takes over model-level NVTX instrumentation, or upstream adds NVTX hooks to qwen3_next. |
| vllm.model_executor.models.qwen3_next.th_nvtx_range_push | module | Ports the fork's NVTX profiling scaffold for qwen3_next; no-op unless VLLM_PPU_NVTX_PROFILE is set and torch.cuda.nvtx imports. | >=0.30.0,<0.31.0 | the vllm_sail.profiling subpackage (Phase 6) takes over model-level NVTX instrumentation, or upstream adds NVTX hooks to qwen3_next. |
| vllm.model_executor.models.step3p5_mtp.Step3p5MTP.load_weights | class | PPU int8/int4 quant checkpoints omit the zero-point buffers, and upstream's completeness check then raises on load. Adds the three zero-point suffixes to the optional-parameter set. Verbatim upstream body with one marked block; the set is method-local. | >=0.30.0,<0.31.0 | upstream's optional_params set in Step3p5MTP.load_weights includes the zero_point suffixes itself. |
| vllm.model_executor.warmup.kernel_warmup.kernel_warmup | module | Upstream's DeepGEMM warmup is gated on support_deep_gemm() (sm90/sm100/sm120), so it never runs on PPU; and CUDA's warmup would not exercise the PPU DeepGEMM wrapper's kernels or tuned configs anyway. Appends the PPU DeepGEMM warmup with the fork's env gates after the upstream body. | >=0.30.0,<0.31.0 | upstream gives kernel_warmup a per-platform warmup registry (or vllm.utils.deep_gemm dispatches on current_platform), so the PPU warmup can register itself. |
| vllm.models.deepseek_v4.attention.DeepseekV4Indexer.__init__ | class | PPU paged logits metadata needs the indexer query head count and dimension. | >=0.30.0,<0.31.0 | DeepseekV4Indexer propagates query dimensions into its MLA cache spec. |
| vllm.models.deepseek_v4.attention.DeepseekV4IndexerCache.__init__ | class | PPU paged logits metadata needs the indexer query head count and dimension. | >=0.30.0,<0.31.0 | DeepseekV4Indexer propagates query dimensions into its MLA cache spec. |
| vllm.models.deepseek_v4.attention.DeepseekV4IndexerCache.get_kv_cache_spec | class | PPU paged logits metadata needs the indexer query head count and dimension. | >=0.30.0,<0.31.0 | DeepseekV4Indexer propagates query dimensions into its MLA cache spec. |
| vllm.models.deepseek_v4.attention.dsa_indexer_uses_fp4 | module | Preloaded indexer consumers must use SAIL's MXFP4 capability. | >=0.30.0,<0.31.0 | Consumers resolve the helper through its provider module. |
| vllm.models.deepseek_v4.attention.fused_indexer_q_rope_quant | module | This DeepSeek V4 consumer captured its indexer operation before PPU installation and would bypass PPU 1.0 INT8 dispatch. | >=0.30.0,<0.31.0 | vLLM resolves DeepSeek V4 indexer operations through module lookups or a registry instead of importing them by value. |
| vllm.models.deepseek_v4.common.ops.cache_utils.dequantize_and_gather_k_cache | module | PPU 1.0 Triton cannot compile the packed cache's hardware FP8 bitcast; use software E4M3 decoding. | >=0.30.0,<0.31.0 | vLLM exposes packed MLA cache kernel registration or supports software FP8 decoding on PPU 1.0. |
| vllm.models.deepseek_v4.common.ops.dequantize_and_gather_k_cache | module | Preloaded V4 cache consumers must use the PPU gather implementation. | >=0.30.0,<0.31.0 | Consumers resolve cache helpers through their provider module. |
| vllm.models.deepseek_v4.common.ops.fused_compress_quant_cache.compress_norm_rope_store_triton | module | PPU 1.0 INT8 indexer queries must read INT8 compressed keys. Upstream writes FP8 bytes using an unsupported fp8e4nv cast; emit signed INT8 bytes and a float32 scale in the same paged layout. The compressor launcher has no backend registration hook. | >=0.30.0,<0.31.0 | vLLM exposes a DeepSeek V4 indexer quantization registration hook or natively supports paired INT8 query and compressed-key quantization on PPU 1.0. |
| vllm.models.deepseek_v4.common.ops.fused_indexer_q.fused_indexer_q_rope_quant | module | PPU 1.0 Triton cannot compile upstream's fp8e4nv indexer query cast. Its DeepGEMM indexer consumes INT8 Q and K; route Q through the PPU INT8 quantizer with its scale folded into the logits weights. The directly called upstream function has no registration hook. | >=0.30.0,<0.31.0 | vLLM exposes a DeepSeek V4 indexer quantization registration hook or natively supports paired INT8 query and compressed-key quantization on PPU 1.0. |
| vllm.models.deepseek_v4.common.ops.fused_indexer_q_rope_quant | module | This DeepSeek V4 consumer captured its indexer operation before PPU installation and would bypass PPU 1.0 INT8 dispatch. | >=0.30.0,<0.31.0 | vLLM resolves DeepSeek V4 indexer operations through module lookups or a registry instead of importing them by value. |
| vllm.models.deepseek_v4.compressor.DeepseekCompressor.forward | class | The upstream head-512 CUDA branch imports CuTeDSL directly; PPU shares CUDA identity but requires the Triton compressor. No compressor backend registration hook is available. | >=0.30.0,<0.31.0 | DeepseekCompressor selects compression through a backend capability hook. |
| vllm.models.deepseek_v4.compressor.compress_norm_rope_store_triton | module | This DeepSeek V4 consumer captured its indexer operation before PPU installation and would bypass PPU 1.0 INT8 dispatch. | >=0.30.0,<0.31.0 | vLLM resolves DeepSeek V4 indexer operations through module lookups or a registry instead of importing them by value. |
| vllm.models.deepseek_v4.nvidia.flashmla.dequantize_and_gather_k_cache | module | Preloaded V4 cache consumers must use the PPU gather implementation. | >=0.30.0,<0.31.0 | Consumers resolve cache helpers through their provider module. |
| vllm.models.deepseek_v4.nvidia.flashmla.flash_mla_sparse_fwd | module | vLLM 0.30 captured the upstream FlashMLA op before PPU patch installation, so the consumer would bypass the PPU provider or retain CUDA-only capability checks. | >=0.30.0,<0.31.0 | vLLM resolves FlashMLA operations through a provider registry or module lookup instead of capturing them by value. |
| vllm.models.deepseek_v4.nvidia.flashmla.flash_mla_with_kvcache | module | vLLM 0.30 captured the upstream FlashMLA op before PPU patch installation, so the consumer would bypass the PPU provider or retain CUDA-only capability checks. | >=0.30.0,<0.31.0 | vLLM resolves FlashMLA operations through a provider registry or module lookup instead of capturing them by value. |
| vllm.models.deepseek_v4.nvidia.model._select_dsv4_attn_cls | module | The upstream DeepSeek V4 selector returns the NVIDIA FlashMLA class. On PPU, substitute the plugin implementation for its DeepGEMM output projection and unpadded head-count contract while preserving every non-PPU and non-FlashMLA selection. | >=0.30.0,<0.31.0 | vLLM exposes a DeepSeek V4 attention-class registration hook, or its selector natively resolves the PPU plugin FlashMLA implementation. |
| vllm.models.kimi_k3.nvidia.kda.KimiK3DeltaAttention.__init__ | class | Kimi KDA must select PPU PLA kernels instead of NVIDIA FlashKDA/native decode. | >=0.30.0,<0.31.0 | Kimi KDA supports platform kernel registration for prefill and decode. |
| vllm.models.kimi_k3.nvidia.kda.KimiK3DeltaAttention._forward | class | Kimi KDA must select PPU PLA kernels instead of NVIDIA FlashKDA/native decode. | >=0.30.0,<0.31.0 | Kimi KDA supports platform kernel registration for prefill and decode. |
| vllm.models.kimi_k3.nvidia.kda.KimiK3DeltaAttention.get_kv_cache_spec | class | Kimi KDA must select PPU PLA kernels instead of NVIDIA FlashKDA/native decode. | >=0.30.0,<0.31.0 | Kimi KDA supports platform kernel registration for prefill and decode. |
| vllm.models.kimi_k3.nvidia.kda._flashkda_prefill | module | Kimi KDA must select PPU PLA kernels instead of NVIDIA FlashKDA/native decode. | >=0.30.0,<0.31.0 | Kimi KDA supports platform kernel registration for prefill and decode. |
| vllm.models.kimi_k3.nvidia.kda.is_flashkda_supported | module | Kimi KDA must select PPU PLA kernels instead of NVIDIA FlashKDA/native decode. | >=0.30.0,<0.31.0 | Kimi KDA supports platform kernel registration for prefill and decode. |
| vllm.models.kimi_k3.nvidia.kda.is_fused_kda_decode_supported | module | Kimi KDA must select PPU PLA kernels instead of NVIDIA FlashKDA/native decode. | >=0.30.0,<0.31.0 | Kimi KDA supports platform kernel registration for prefill and decode. |
| vllm.platforms.interface.Platform.is_ppu | class | vLLM has no PlatformEnum.PPU member and no is_ppu() predicate. PPU code throughout this plugin (and the ~101 call sites inherited from the in-tree PPU fork) branches on current_platform.is_ppu(), so the predicate must exist on the base Platform class for every platform, returning False for non-PPU platforms. | >=0.30.0,<0.31.0 | vLLM gains a first-class way for an out-of-tree platform to declare a vendor identity that is queryable without an enum member (e.g. a `Platform.vendor` field), or upstream accepts PlatformEnum.PPU. |
| vllm.platforms.interface.Platform.is_sleep_mode_available | class | Upstream gates sleep mode on `self._enum in (CUDA, ROCM, XPU)`. PPU declares PlatformEnum.CUDA so it already passes, but the in-tree fork listed PPU explicitly and we keep the behaviour pinned here so that an upstream change to the gate (e.g. to a capability probe) surfaces as a patch-drift failure rather than silently disabling sleep mode on PPU. | >=0.30.0,<0.31.0 | upstream replaces the enum allow-list with a capability query that PPU answers correctly by inheritance. |
| vllm.third_party.flash_linear_attention.ops.fused_recurrent.fused_recurrent_gated_delta_rule_packed_decode | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.third_party.flash_linear_attention.ops.fused_recurrent_gated_delta_rule_packed_decode | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.third_party.flash_linear_attention.ops.fused_sigmoid_gating.fused_sigmoid_gating_delta_rule_update | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.third_party.flash_linear_attention.ops.fused_sigmoid_gating_delta_rule_update | module | PPU GDN uses PLA for supported decode/prefill layouts and Triton for other shapes. | >=0.30.0,<0.31.0 | GDN exposes platform backend registration for PLA prefill and recurrent kernels. |
| vllm.utils.import_utils.has_cutedsl | module | CuTeDSL kernels do not run on PPU. Upstream only checks that the `cutlass` module imports, and PPU passes the platform gate because it declares PlatformEnum.CUDA, so vLLM would select CuTeDSL kernels that fail at launch. | >=0.30.0,<0.31.0 | PPU gains CuTeDSL support, or upstream gates this on a real device capability. |
| vllm.utils.import_utils.has_humming | module | Humming kernels do not run on PPU; same reasoning as has_cutedsl above. | >=0.30.0,<0.31.0 | PPU gains Humming support, or upstream gates this on a real device capability. |
| vllm.v1.attention.backends.fa_utils.flash_attn_supports_kv_cache_dtype | module | PPU 1.5 FA3 supports FP8 descales; the NVIDIA SM90 gate excludes PPU. | >=0.30.0,<0.31.0 | The FlashAttention capability resolver supports PPU through a platform hook. |
| vllm.v1.attention.backends.fa_utils.get_flash_attn_version | module | PPU prefers FA3 before applying configuration and compatibility fallbacks, including ALiBi. | >=0.30.0,<0.31.0 | The FlashAttention capability resolver supports PPU through a platform hook. |
| vllm.v1.attention.backends.flash_attn.FlashAttentionBackend.supports_combination | class | The upstream sink combination gate assumes only NVIDIA SM90 supports FA3 sinks. | >=0.30.0,<0.31.0 | FlashAttentionBackend gates sinks through the selected implementation's capabilities. |
| vllm.v1.attention.backends.mla.flashmla.FlashMLABackend.supports_compute_capability | class | PPU runs FlashMLA on capability major 8. Upstream accepts only majors 9 and 10, so without this PPU could never select the FlashMLA backend. | >=0.30.0,<0.31.0 | upstream accepts PPU's capability for FlashMLA natively. |
| vllm.v1.attention.backends.mla.flashmla.FlashMLAImpl.forward_mqa | class | PPU NVTX profiling: the fork wraps the FlashMLA decode kernel call in forward_mqa with an [FM_FMHA] NVTX range gated by VLLM_PPU_NVTX_PROFILE. The plugin pushes the range around the upstream method instead; the label contents follow the fork. | >=0.30.0,<0.31.0 | PPU profiling moves to a mechanism that does not need NVTX ranges here. |
| vllm.v1.attention.backends.mla.flashmla.FlashMLAMetadataBuilder.__init__ | class | PPU FIXME (kai): the fork hard-codes num_sms = 320 for the PPU MLA tile-scheduler buffers ('temporarily use hard-coded ppu magic number, this is align with ppu mla algo') instead of querying the device. | >=0.30.0,<0.31.0 | PPU reports a compute-unit count the FlashMLA scheduler can use directly, and the fork's hard-coded 320 is removed. |
| vllm.v1.attention.backends.mla.flashmla_sparse.FlashMLASparseBackend.supports_compute_capability | class | PPU runs sparse FlashMLA on capability major 8. Upstream accepts only majors 9 and 10, so without this PPU could never select the sparse FlashMLA backend. | >=0.30.0,<0.31.0 | upstream accepts PPU's capability for sparse FlashMLA natively. |
| vllm.v1.attention.backends.mla.flashmla_sparse.FlashMLASparseMetadataBuilder.__init__ | class | PPU uses the full SM count for max_num_sm_parts (the fork: 'PPU uses full SM count') instead of the SM90/SM100 formulas. The value only sizes tile_scheduler_metadata_buffer, so this delegates to upstream and re-allocates that buffer on PPU. | >=0.30.0,<0.31.0 | upstream grows a PPU branch in the max_num_sm_parts formula, or the PPU scheduler stops needing the full SM count. |
| vllm.v1.attention.backends.mla.indexer.DeepseekV32IndexerMetadataBuilder.__init__ | class | PPU branches from the fork: fp4 indexer cache allowed on sm_89, indexer_n_head/indexer_q_head_dim read from the KV-cache spec on PPU, SM count from PPU DeepGEMM's get_num_sms, and the enlarged num_sms * PPU_DEEP_GEMM_TB_PER_CU scheduler-metadata buffer for fp4 paged logits. Delegation cannot intercept the upstream capability assert, hence the verbatim body with only the fork's changes marked. | >=0.30.0,<0.31.0 | upstream grows PPU branches in the DSA indexer metadata builder. |
| vllm.v1.attention.backends.mla.indexer.DeepseekV32IndexerMetadataBuilder._split_indexer_prefill_chunks | class | Fork adds a logits_dtype parameter so the fp4 indexer cache (bf16 logits) chunks against the correct logits budget. UNCONDITIONAL in the fork: the default path also changes (max_logits_elems clamped to >= 1); reproduced as-in-fork and flagged in the Phase-4 report. | >=0.30.0,<0.31.0 | upstream adds the logits_dtype parameter itself. |
| vllm.v1.attention.backends.mla.indexer.DeepseekV32IndexerMetadataBuilder.build | class | Fork's DSA indexer build path for PPU: pass logits_dtype to the prefill chunk splitter, and build PPU paged-MQA-logits scheduler metadata with the metadata_extra tuple (next_n, indexer_n_head, indexer_q_head_dim, PPU_FP4_INDEXER_ELEMENT_SIZE), slicing the scheduler buffer to the returned size. Verbatim body with only the fork's changes marked. | >=0.30.0,<0.31.0 | upstream grows PPU branches in the DSA indexer build path. |
| vllm.v1.attention.backends.mla.indexer.PPU_DEEP_GEMM_TB_PER_CU | value | Fork constant: thread blocks per compute unit used by PPU DeepGEMM when sizing the fp4 indexer scheduler-metadata buffer. | >=0.30.0,<0.31.0 | upstream defines the constant itself (patch_value becomes a no-op). |
| vllm.v1.attention.backends.mla.indexer.PPU_FP4_INDEXER_ELEMENT_SIZE | value | Fork constant: element size (bytes) of the PPU fp4 indexer cache, passed to the PPU paged MQA logits metadata builder via metadata_extra. | >=0.30.0,<0.31.0 | upstream defines the constant itself (patch_value becomes a no-op). |
| vllm.v1.attention.backends.mla.indexer.dsa_indexer_uses_fp4 | module | PPU MXFP4 indexer support is supplied by SAIL DeepGEMM, not CUDA SM100. | >=0.30.0,<0.31.0 | The indexer exposes a platform capability hook. |
| vllm.v1.attention.backends.mla.indexer.get_paged_mqa_logits_metadata | module | Fork's is_ppu() import switch selects the PPU DeepGEMM wrapper's get_paged_mqa_logits_metadata, which accepts the PPU metadata_extra tuple. The CUDA helper has no such parameter. | >=0.30.0,<0.31.0 | vllm.utils.deep_gemm dispatches on current_platform internally, or upstream accepts a DeepGEMM backend registry. |
| vllm.v1.attention.backends.mla.sparse_swa.get_mla_metadata | module | vLLM 0.30 captured the upstream FlashMLA op before PPU patch installation, so the consumer would bypass the PPU provider or retain CUDA-only capability checks. | >=0.30.0,<0.31.0 | vLLM resolves FlashMLA operations through a provider registry or module lookup instead of capturing them by value. |
| vllm.v1.attention.ops.flashmla._is_flashmla_available | module | PPU ships the upstream flash_mla package instead of vLLM's compiled _flashmla_C/_flashmla_extension_C extensions. Upstream reports FlashMLA unavailable unless those extensions imported, so on PPU the check must look for flash_mla instead. | >=0.30.0,<0.31.0 | PPU builds vLLM's FlashMLA extensions, or upstream grows a pluggable FlashMLA provider. |
| vllm.v1.attention.ops.flashmla.flash_mla_sparse_fwd | module | On PPU the sparse FlashMLA entry point comes from the upstream flash_mla package, not from vllm.third_party.flashmla (which wraps the compiled CUDA extensions PPU does not build). Lazy import keeps non-PPU hosts and hosts without flash_mla unaffected. | >=0.30.0,<0.31.0 | PPU builds vLLM's FlashMLA extensions, or upstream grows a pluggable FlashMLA provider. |
| vllm.v1.attention.ops.flashmla.flash_mla_with_kvcache | module | On PPU flash_mla_with_kvcache comes from the upstream flash_mla package rather than vllm.third_party.flashmla. Same reasoning as flash_mla_sparse_fwd. | >=0.30.0,<0.31.0 | PPU builds vLLM's FlashMLA extensions, or upstream grows a pluggable FlashMLA provider. |
| vllm.v1.attention.ops.flashmla.flash_mla_with_kvcache_fp8 | module | PPU Note(kai): PPU flash mla limit — PPU has no FP8 dense FlashMLA kernel (it would call the _flashmla_extension_C op PPU does not build), so the fork raises 'FlashMLA is not available' on PPU. | >=0.30.0,<0.31.0 | PPU supports the FP8 dense FlashMLA decode path. |
| vllm.v1.attention.ops.flashmla.get_mla_metadata | module | On PPU get_mla_metadata comes from the upstream flash_mla package rather than vllm.third_party.flashmla. Same reasoning as flash_mla_sparse_fwd. | >=0.30.0,<0.31.0 | PPU builds vLLM's FlashMLA extensions, or upstream grows a pluggable FlashMLA provider. |
| vllm.v1.attention.ops.flashmla.get_mla_metadata_dense_fp8 | module | PPU Note(kai): PPU flash mla limit — PPU has no FP8 dense FlashMLA metadata path (it would call the _flashmla_extension_C op PPU does not build), so the fork raises 'FlashMLA is not available' on PPU. | >=0.30.0,<0.31.0 | PPU supports the FP8 dense FlashMLA decode path. |
| vllm.v1.attention.ops.flashmla.is_flashmla_dense_supported | module | PPU supports dense FlashMLA on its own capability; upstream restricts it to Hopper (family 90). The fork returns (True, None) on PPU once flash_mla is available. | >=0.30.0,<0.31.0 | upstream's capability check accepts PPU for dense FlashMLA. |
| vllm.v1.attention.ops.flashmla.is_flashmla_sparse_supported | module | PPU supports sparse FlashMLA on its own capability; upstream restricts it to Hopper and Blackwell datacenter devices. The fork returns (True, None) on PPU once flash_mla is available. | >=0.30.0,<0.31.0 | upstream's capability check accepts PPU for sparse FlashMLA. |
| vllm.v1.attention.ops.triton_decode_attention._decode_grouped_att_m_fwd | module | PPU BTV 1.1 (capability 8.0) needs BLOCK=16 for Lk>=576 grouped decode attention (fork's is_btv_1_1 branch), and the fork removes the 'not is_hip_' guards from the MLA tile branches — the latter is an unconditional upstream behaviour change (also affects ROCm), kept as-in-fork. Module-level platform flags from the fork are resolved lazily inside the body per plugin rules. | >=0.30.0,<0.31.0 | upstream adopts the PPU block-size workaround and the ROCm tile-layout change, or the fork drops its is_btv_1_1/is_hip_ edits. |
| vllm.v1.kv_cache_interface.HiddenStateCacheSpec.__eq__ | class | Existing MLA spec subclasses must retain geometry through dataclass operations. | >=0.30.0,<0.31.0 | Upstream declares the indexer geometry dataclass fields. |
| vllm.v1.kv_cache_interface.HiddenStateCacheSpec.__hash__ | class | Existing MLA spec subclasses must retain geometry through dataclass operations. | >=0.30.0,<0.31.0 | Upstream declares the indexer geometry dataclass fields. |
| vllm.v1.kv_cache_interface.HiddenStateCacheSpec.__init__ | class | Existing MLA spec subclasses must retain geometry through dataclass operations. | >=0.30.0,<0.31.0 | Upstream declares the indexer geometry dataclass fields. |
| vllm.v1.kv_cache_interface.MLAAttentionSpec.__eq__ | class | Indexer geometry participates in KV spec grouping as in the PPU fork. | >=0.30.0,<0.31.0 | Upstream declares the indexer geometry dataclass fields. |
| vllm.v1.kv_cache_interface.MLAAttentionSpec.__hash__ | class | Hash indexer geometry consistently with MLA spec equality. | >=0.30.0,<0.31.0 | Upstream declares the indexer geometry dataclass fields. |
| vllm.v1.kv_cache_interface.MLAAttentionSpec.__init__ | class | Fork adds indexer_n_head/indexer_q_head_dim fields to MLAAttentionSpec so the DeepseekV4 indexer cache can publish its geometry to the DSA indexer metadata builder. Field descriptors are extended alongside the generated constructor so dataclasses.replace retains them: it accepts the two extra kwargs, delegates the rest, and stores them via object.__setattr__. | >=0.30.0,<0.31.0 | upstream adds the indexer geometry fields to MLAAttentionSpec. |
| vllm.v1.kv_cache_interface.MLAAttentionSpec.merge | class | Fork forwards indexer_n_head/indexer_q_head_dim through MLAAttentionSpec.merge (from specs[0], like the other geometry fields); DeepseekV32IndexerMetadataBuilder reads them off the merged group spec. Delegates to upstream merge, then copies the values from specs[0] onto the merged (frozen) spec. | >=0.30.0,<0.31.0 | upstream adds the indexer geometry fields to MLAAttentionSpec. |
| vllm.v1.worker.gpu.spec_decode.rejection_sampler.rejection_sample | module | PPU must reinitialize batches on context-length changes and insert speculative tokens within one warp. | >=0.30.0,<0.31.0 | Upstream includes the context-length invalidation and one-warp insertion fixes. |
| vllm.v1.worker.gpu.spec_decode.rejection_sampler_utils.rejection_sample | module | PPU must reinitialize batches on context-length changes and insert speculative tokens within one warp. | >=0.30.0,<0.31.0 | Upstream includes the context-length invalidation and one-warp insertion fixes. |
| vllm.v1.worker.gpu_model_runner.GPUModelRunner.may_reinitialize_input_batch | class | PPU must reinitialize batches on context-length changes and insert speculative tokens within one warp. | >=0.30.0,<0.31.0 | Upstream includes the context-length invalidation and one-warp insertion fixes. |
| vllm.v1.worker.gpu_worker.Worker._scoped_allocator_max_split | class | PPU torch raises the CachingAllocator minimum for max_split_size_mb from 20 to 32 MiB; upstream's Worker.load_model requests 20, which PPU's allocator rejects during weight loading. | >=0.30.0,<0.31.0 | PPU torch accepts max_split_size_mb=20 again (its CachingAllocator floor matches stock PyTorch), or upstream makes the floor platform-queryable. |
